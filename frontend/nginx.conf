server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # React Router handles routing on the client side.
    # If a route is not found as a file, serve index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}