# backend/app/services/__init__.py

# Empty for now — we import services directly where needed:
# from app.services import user_service
#
# (We keep this file minimal since services are typically
# imported as modules, not individual functions, to keep
# the calling code readable: user_service.create_user(...))